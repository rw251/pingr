mixin header(item, classIn)
  //th.sortable(style='min-width:90px', class=item.direction classIn, data-toggle='tooltip', title=item.tooltip)
  th.sortable(class=item.direction =classIn, data-toggle='tooltip', title=item.tooltip)
    if item.titleHTML
      !=item.titleHTML
    else 
      !=item.title + ' '
      
    //- if !item.isUnSortable
    //-   if item.isSorted
    //-     if item.isAsc
    //-       i.fa.fa-sort-asc(data-toggle='tooltip', title='Order patients by'+item.title)
    //-     else
    //-       i.fa.fa-sort-desc(data-toggle='tooltip', title='Order patients by'+item.title)
    //-   else
    //-     i.fa.fa-sort(data-toggle='tooltip', title='Order patients by'+item.title)
