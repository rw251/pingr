.card
  .card-header.card-header-icon(data-background-color="purple")
    i.material-icons assistant
  .card-content
    h4.card-title Suggested Actions
    small.category= "Total Suggested Actions" + (indicator ? " for indicator '" + indicator + "'" : "" )  + " For this Patient"
    .row
      .panel.panel-default
        #advice-list
.card
  .card-header.card-header-icon(data-background-color="green")
    i.material-icons assignment_ind
  .card-content
    h4.card-title Suggestion you have made
    small.category= "These suggestions are shared with your team so everyone can benefit"
    #tab-plan-individual.panel.panel-default
      #personalPlanIndividual
      
        
        
//the modals have moved to the modal container
//include mixins/edit-plan-modal
//include mixins/delete-plan-modal
//BG-TODO user added plans in separate panel so always visible on screen
// also consider renaming panels inline with below
//- =======
//- .panel.panel-default(style='margin-bottom:10px')
//-   .clearfix.panel-heading= "Top 3 suggested actions" + (indicator ? " for indicator '" + indicator + "'" : "" )  + " for patient with NHS number " + nhs
//-   #individual-action-panel.panel-body(style='padding:0')
//-     #advice-list(style='overflow-y:auto')
//- .panel.panel-default(style='margin-bottom:5px')
//-   #user-action-panel.panel-body
//-     #personalPlanIndividual(style='overflow-y:auto')
//- include mixins/edit-plan-modal
//- include mixins/delete-plan-modal
//- >>>>>>> dev
