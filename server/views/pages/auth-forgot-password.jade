include ../partials/head.jade
include ../mixins/toolbar.jade
.container-narrow.starter-template-high
  +toolbar('Reset password', [{btn:'btn-info',path:'/',text:'Back to PINGR'}])
  form.signin.form-horizontal(action='/auth/reset', method='post')
    p
      | Please enter your email and click 'Send reset link'.  We will then email you details of how to change your password.
    .form-group
      label.col-md-4.control-label(for='email') Email
      .col-md-8
        input#email.form-control(required='', type='email', name='email', placeholder='Email')
    .form-group
      .col-md-offset-4.col-md-8
        button.btn.btn-primary(type='submit') Send reset link
    if message && message.success
      .text-center.text-success
        strong= message.success
    if message && message.error
      .text-center.text-danger
        strong= message.error
include ../partials/footerWithTracking.jade